sudo: required

language: java

services:
  - docker
  
env:
  global:
     - COMMIT=${TRAVIS_COMMIT::6}  

script:
 echo "No tests!" 
 
after_success:
 - docker login -u="$DOCKER_USERNAME" -p="$DOCKER_PASSWORD"

